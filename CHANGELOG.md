# Changelog

## 1.0.0
- Converted the project to a pure TypeScript library and CLI published via npm
- Added type declaration output and `exports` metadata for consumers
- Documented installation, CLI usage, and quickstart flows for Node.js users
- Removed legacy Rust-focused guides to keep the docs aligned with the npm package
